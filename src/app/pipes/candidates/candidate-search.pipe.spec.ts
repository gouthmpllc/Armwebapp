import { CandidateSearchPipe } from './candidate-search.pipe';

describe('CandidateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
