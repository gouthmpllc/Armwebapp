import { ReportsSearchPipe } from './reports-search.pipe';

describe('ReportsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
